{
  "name": "whitelablelogin",
  "version": "0.0.1",
  "description": "Login page that depending on the user preference it will privide the proper app. ie: while label",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mkl",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "git-cz": "^3.0.1"
  }
}
